/**Miscellaneous Programs
 * 1. boolean isLeapYear(int) //checks if a year is leap year(true) or not(false).
 * 2. 
 */
class Misc{
    boolean checkLeapYear(int yr){
        if(yr%4==0)
        return true;
        return false;
    }
}